# Triage Labels

This repo uses the default label vocabulary:

- `needs-triage`
- `needs-info`
- `ready-for-agent`
- `ready-for-human`
- `wontfix`

If the repo later adopts different label names, update this file so the triage workflow matches the real labels instead of creating duplicates.
